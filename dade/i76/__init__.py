"""
Interstate '76 and Interstate '82 (Activision) asset toolkit.

Readers and decoders for the formats shipped with both games: the ZFSF and ZFS3 archives and their
LZO compression, ``.pak`` bundles, the ``.act``, ``.map``, ``.vqm``, and ``.cbk`` texture formats,
``.geo`` meshes and the ``.sdf`` models that assemble them, BWD2 chunk containers, mission horizon
panoramas, and the custom executable packer used by ``i82sim.dll``.
"""
