"""
Max Payne (Remedy Entertainment, 2001) asset toolkit.

Readers for the formats built on Remedy's ``rl`` library: the RAS (Remedy Archive System) ``.ras``
containers and the ``.mpm`` mod packages that share their layout, the seeded stream cipher and
``RA->``/``RC->`` block wrappers guarding their contents, and the tagged ``R_MemoryFile`` streams
that carry every custom asset, including ``.ldb`` levels and ``.ai`` movement networks.
"""
